CREATE table Sensor(
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
    name varchar(100) NOT NULL UNIQUE
);

CREATE TABLE Measurement(
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
    sensor_id int REFERENCES Sensor(id) ON DELETE CASCADE,
    value real check (value > -100 AND value < 100),
    raining boolean
);